# Secretos — nunca al repo
.env
.env.*
!.env.example
!*.enc.*
*.pem
*.key
age-key*.txt

# Modelos y pesos — se descargan, no se versionan (gigas)
*.gguf
*.safetensors
*.bin
models/

# Datos del usuario — privados por diseño
data/
sessions/
qdrant_storage/
datasets/corrections/
logs/
*.log

# Bitácora privada del asistente (notas de sesión, sobrevive a faltas de contexto)
bitacora/

# Entornos y caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
node_modules/

# Material de gestión interna — no se publica (solo /prompts raíz, no roles/*/prompts)
/prompts/

# Dataset de identidad de Hefisty — COMPLETO privado (se publica solo la IA entrenada,
# nunca el material de entrenamiento). Respaldar fuera de GitHub.
roles/hefisty/dataset/

# Material privado/sensible — NUNCA se sube (repo público): fuentes de terceros para dar
# contexto o entrenar a la IA, contexto interno, credenciales. Todo va bajo secret/.
secret/

# SO / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
