# Supply-chain hardening: refuse package versions published less than 1 day
# (1440 minutes) ago. pnpm resolves to the newest version within range that is
# at least this old, mitigating fresh malicious releases.
minimum-release-age=1440
